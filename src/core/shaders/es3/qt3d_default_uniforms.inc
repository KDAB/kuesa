uniform highp mat4 viewMatrix;
uniform highp mat4 projectionMatrix;
uniform highp mat4 viewProjectionMatrix;
uniform highp mat4 inverseViewMatrix;
uniform highp mat4 inverseProjectionMatrix;
uniform highp mat4 inverseViewProjectionMatrix;
uniform highp mat4 viewportMatrix;
uniform highp mat4 inverseViewportMatrix;
uniform highp vec4 textureTransformMatrix;
uniform highp vec3 eyePosition;
uniform highp float aspectRatio;
uniform highp float gamma;
uniform highp float exposure;
uniform highp float time;

uniform highp mat4 modelMatrix;
uniform highp mat4 inverseModelMatrix;
uniform highp mat4 modelViewMatrix;
uniform highp mat3 modelNormalMatrix;
uniform highp mat4 inverseModelViewMatrix;
uniform highp mat4 mvp;
uniform highp mat4 inverseModelViewProjectionMatrix;
